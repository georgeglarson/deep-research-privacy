import { describe, it } from 'node:test';
import assert from 'node:assert';

// Basic test to get CI passing
// TODO: Add more comprehensive tests
describe('Text Splitter', () => {
  it('should pass initial CI', () => {
    assert.strictEqual(true, true);
  });
});